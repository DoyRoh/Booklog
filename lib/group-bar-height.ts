// 고정 상단 그룹 전환 바의 높이(px). 페이지 본문이 이만큼 위쪽 여백을
// 비워 둬야 바에 가려지지 않는다.
//
// 반드시 이 "use client"가 아닌 모듈에 둔다: "use client" 컴포넌트 파일에서
// export한 값을 서버 컴포넌트가 import하면 숫자가 아니라 클라이언트 참조
// 프록시가 넘어와서(`32 + H` → "32function() {…"), 인라인 padding이 통째로
// 무시되고 본문이 바 뒤로 숨는다(실제로 겪음 -- "그룹 탭에 추천도서/숙제
// 구분이 안 보인다").
//
// 값은 실제 아이폰(글자 크기 112.5%)에서 잰 높이보다 조금 넉넉하게 잡는다.
// 그룹 타일 줄(56px 타일 + 숫자 배지 + 이름) + 숙제/추천도서 소제목
// 탭 줄(밑줄형에서 채워진 알약 세그먼트 컨트롤로 바꾸면서 세로 패딩이
// 조금 늘었다, +검색 아이콘). 정적 HTML 복제본을 360/390/430px에서
// Playwright로 실측(125px, 세그먼트 컨트롤 반영 전)한 값에 실제 폰트
// (Gowun Dodum) 렌더링 오차 + 세그먼트 패딩 증가분을 감안한 여유를 더했다.
export const CHILD_GROUP_BAR_HEIGHT = 148;
export const OPERATOR_GROUP_BAR_HEIGHT = 82;
