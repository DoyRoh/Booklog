# 책숲

아이의 독서를 부모·교사, 그리고 책을 추천하는 기관·크리에이터가 함께 기록하고 넓혀가는 어린이 독서 플랫폼.

Next.js(App Router) + TypeScript + Supabase 기반. 자세한 제품/스키마 설계는 `CLAUDE.md`를 참고하세요.

이 저장소의 `legacy/index.html`은 서비스형으로 확장하기 전의 개인용 프로토타입("유안이 독서기록")으로, 디자인 시스템과 UI 참고용으로만 남겨둔 것이며 빌드에는 포함되지 않습니다.

## 로컬 실행 방법

### 1. 의존성 설치

```bash
npm install
```

### 2. Supabase 프로젝트 준비

1. [supabase.com](https://supabase.com)에서 새 프로젝트를 생성합니다.
2. `.env.example`을 복사해 `.env.local`을 만들고, Supabase 프로젝트의 **Settings → API**에서 값을 채웁니다.

   ```bash
   cp .env.example .env.local
   ```

   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

3. `supabase/migrations`의 마이그레이션을 프로젝트에 적용합니다. [Supabase CLI](https://supabase.com/docs/guides/local-development/cli/getting-started)를 사용하는 경우:

   ```bash
   npx supabase login
   npx supabase link --project-ref <project-ref>
   npx supabase db push
   ```

   CLI를 쓰지 않는다면 Supabase 대시보드의 **SQL Editor**에서 `supabase/migrations` 안의 `0001_init_schema.sql`부터 `0005_phase4_groups.sql`까지 순서대로 실행해도 됩니다.

4. **Authentication → Providers**에서 이메일 로그인이 켜져 있는지 확인합니다. (기본값으로 켜져 있습니다.) 로컬 개발 중에는 **Authentication → Email**에서 "Confirm email"을 꺼두면 가입 즉시 로그인 테스트를 할 수 있습니다.

### 3. 카카오 책 검색 API 키 발급 (Phase 2)

바코드 스캔·ISBN 조회로 책 표지/제목/저자를 자동으로 가져오는 데 씁니다.

1. [카카오 개발자](https://developers.kakao.com)에 로그인 후 **내 애플리케이션 → 애플리케이션 추가하기**로 앱을 하나 만듭니다.
2. 만든 앱 → **앱 키**에서 **REST API 키**를 복사합니다.
3. `.env.local`에 추가합니다.

   ```env
   KAKAO_REST_API_KEY=여기에-REST-API-키
   ```

   `NEXT_PUBLIC_` 접두사가 없는 서버 전용 값입니다 — 브라우저에는 절대 노출되지 않고, `/api/books/lookup` 라우트에서만 사용됩니다. 별도 권한 설정 없이 REST API 키만 있으면 책 검색 API를 바로 쓸 수 있습니다.

### 3-1. 국립어린이청소년도서관 사서추천도서 API 연동 (선택)

국립어린이청소년도서관의 사서추천도서를 큐레이터 계정 하나로 자동 등록하는 기능입니다. 이 단계 없이도 앱은 정상 동작하며, 건너뛰어도 됩니다.

1. [문화공공데이터광장](https://www.culture.go.kr/data/openapi/openapiView.do?id=135)에서 "국립어린이청소년도서관_사서추천도서" API 활용신청을 하고 인증키를 발급받습니다(대부분 즉시 승인됩니다).
2. `/signup`에서 이 큐레이터 계정 전용 이메일(예: `nlcy-curator@yourdomain.com`)로 회원가입합니다. 온보딩까지 마칠 필요는 없습니다.
3. `supabase/seed/nlcy_curator.sql`을 열어 상단의 `v_curator_email`을 방금 가입한 이메일로 바꾼 뒤(마이그레이션 0014까지 먼저 적용돼 있어야 합니다), Supabase SQL Editor에서 실행합니다. `role='curator'`로 바뀌고, "국립어린이청소년도서관" 그룹과 "사서추천도서" 목록이 만들어집니다.
4. `.env.local`(로컬 실행 시) 및 Vercel 프로젝트 환경변수(배포 시)에 아래 값을 채웁니다.

   ```env
   NLCY_SERVICE_KEY=발급받은-KCISA-인증키
   NLCY_CURATOR_EMAIL=2번에서-가입한-이메일
   NLCY_CURATOR_PASSWORD=2번에서-정한-비밀번호
   CRON_SECRET=아무-긴-임의-문자열
   ```

5. Vercel에 배포하면 `vercel.json`에 등록된 Cron이 매일 `/api/cron/nlcy-sync`를 자동으로 호출합니다. 지금 바로 확인하고 싶다면, 위 3~4단계까지 마친 계정으로 로그인해 `/curator` 대시보드의 "지금 동기화" 버튼을 눌러도 됩니다.

### 4. 개발 서버 실행

```bash
npm run dev
```

[http://localhost:3000](http://localhost:3000)에서 확인합니다. `/`는 `/today`로 리다이렉트되며, 하단 5탭(오늘/책장/기록/추천/더보기) 네비게이션이 보여야 합니다.

### 5. 회원가입 → 온보딩 → 로그인 → 로그아웃 확인

로그인하지 않은 상태로 `/today` 등 탭 경로에 들어가면 자동으로 `/login`으로 이동합니다. 로그인했지만 온보딩(역할 선택·약관 동의·아이 등록)을 마치지 않았다면 자동으로 `/onboarding`으로 이동합니다.

1. `/signup`에서 이메일/비밀번호로 가입합니다.
2. (이메일 확인을 켜둔 경우) 받은 메일의 링크를 클릭해 계정을 확인합니다.
3. `/login`에서 로그인하면 `/onboarding`으로 이동합니다.
4. **역할 선택**(부모/교사/큐레이터) → **약관 동의**(필수 이용약관, 선택 음성녹음) → (부모인 경우) **아이 등록**(이름, 생년월일, 아바타) 순서로 진행하고 "시작하기"를 누르면 `/today`로 이동합니다.
5. 온보딩을 마친 뒤 `/login`이나 `/signup`에 다시 들어가면 자동으로 `/today`로 리다이렉트되는지 확인합니다.
6. 하단 탭의 **더보기**에서 로그인한 이메일이 보이고, **로그아웃** 버튼이 동작하는지 확인합니다.

### 6. RLS 수동 검증

서로 다른 두 계정(예: 부모 A, 부모 B)으로 각각 아이(`children`)를 등록하고, `child_guardians`로 연결한 뒤:

- 부모 A 계정으로는 자신이 등록한 아이와 독서기록만 조회/수정되어야 합니다.
- 부모 B 계정으로 부모 A의 아이 데이터에 접근하면 빈 결과(RLS에 의해 차단)여야 합니다.
- 교사 계정으로 학급형 그룹을 만들고 부모 계정이 가입 승인을 받은 뒤, `teacher_reading_view`를 통해서만 최소한의 독서기록 컬럼이 보이는지 확인합니다.

이 시나리오는 실제로 로컬 Postgres(mock auth 스키마)에 두 마이그레이션을 적용해 부모A/부모B/교사 세 계정으로 검증했습니다. 그 과정에서 다음 두 가지 버그를 실제로 발견해 고쳤습니다:

1. **RLS 무한 재귀**: `child_guardians`와 `group_members`가 "같은 그룹/아이의 다른 보호자 보기" 같은 정책에서 자기 자신을 다시 조회하고, `groups` ↔ `group_members`가 서로를 참조하면서 순환이 생겨 `infinite recursion detected in policy` 오류가 났습니다. → `SECURITY DEFINER` 헬퍼 함수(`is_child_guardian`, `has_group_role`, `is_approved_group_participant`)로 내부 조회가 RLS를 다시 타지 않도록 고쳤습니다.
2. **가입 신청 자체가 막히는 문제**: `group_members` INSERT 정책이 `groups` 테이블을 직접 조회해 `join_policy`를 확인했는데, approval형 그룹은 승인되기 전엔 애초에 SELECT로 보이지 않아 신청 자체가 거부됐습니다. → `group_join_policy()` 헬퍼 함수로 RLS를 우회해 `join_policy`만 조회하도록 고쳤습니다. (그룹 존재 여부·가입방식만 노출되며 민감 정보는 아닙니다.)

**알려진 한계(Phase 3~4에서 다룰 것)**: `reading_records.group_id`는 보호자가 자유롭게 값을 넣을 수 있고, 실제로 그 그룹에 승인된 상태인지는 검증하지 않습니다. 즉 이론적으로 부모가 자신의 독서기록에 임의의 `group_id`를 지정하면, 그 그룹의 교사가 `teacher_reading_view`로 승인되지 않은 아이의 기록까지 볼 수 있습니다. 독서기록에 그룹을 연결하는 실제 기능이 들어오는 Phase 3~4에서, 승인된 멤버십을 검증하는 트리거나 서버 로직으로 막아야 합니다.

### Phase 1 추가 검증: 권한 상승 방지

`users` 테이블은 로그인한 사용자가 자기 행(row)을 수정할 수 있게 되어있는데(온보딩에서 역할을 저장하려면 필요), 값 제한이 없으면 누구나 자기 `role`을 `'admin'`으로 바꿔치기할 수 있는 구멍이 됩니다. 로컬 Postgres에서 실제로 `update users set role = 'admin' where id = auth.uid()`를 시도해 RLS가 막는지(`new row violates row-level security policy`), 그리고 `role`을 `parent`/`teacher`/`curator`로 바꾸는 정상 온보딩 업데이트는 성공하는지 확인했습니다.

### 7. Phase 2 확인: 책 등록 (바코드 스캔 / ISBN 직접 입력)

**책장** 탭 → **"+ 책 등록"**에서:

1. **카메라로 바코드 스캔**: 실제 스마트폰 브라우저로 접속해야 카메라 권한 요청까지 제대로 테스트됩니다 (노트북 웹캠도 가능하지만, 실사용 시나리오는 폰 카메라입니다). `localhost`는 보안 컨텍스트로 취급되어 로컬 개발 중에도 카메라 권한 자체는 동작하지만, 폰에서 접속하려면 로컬 서버를 배포하거나(Vercel) 같은 와이파이에서 `http://<PC의 사설IP>:3000`으로 접속해야 합니다 (`npm run dev` 실행 시 뜨는 "Network:" 주소).
2. **ISBN 직접 입력**: 카메라 없이도 바로 테스트 가능합니다. 아무 책 뒷면의 13자리 숫자(ISBN)를 입력하면 카카오 책 검색 API로 표지·제목·저자를 가져와 미리보기를 보여줍니다.
3. **중복 방지**: 이미 등록된 ISBN을 다시 입력하면 카카오 API를 다시 부르지 않고 기존 책 정보로 바로 기록 단계(아래)로 넘어갑니다 (`book_isbns.isbn`이 DB 레벨에서도 `unique` 제약이라 이중으로 막혀 있습니다).

`KAKAO_REST_API_KEY`를 설정하지 않으면 조회 단계에서 에러가 뜨는데, 정상입니다 — 위 3번 단계를 따라 키를 발급해 넣어주세요.

### 8. Phase 3 확인: 독서기록 + 다자녀 전환

**아이 등록·전환** (더보기 탭):

1. 더보기 탭에 로그인한 이메일, 역할, 등록된 아이 목록이 보이는지 확인합니다.
2. **"+ 아이 추가"**로 두 번째 아이를 등록합니다 (이름/생년월일/아바타).
3. 아이 카드를 탭하면 "선택됨" 표시가 옮겨가는지 확인합니다 — 이게 지금부터 책장·기록에 표시될 아이입니다.

**독서기록 남기기** (책장 탭 → "+ 책 등록"):

1. 책을 스캔/입력해서 조회하면, 이제 미리보기 화면에 **평점(1~5) · 기분 · 즐겨찾기 · 부모 메모** 입력이 함께 뜹니다.
2. "기록 남기기"를 누르면 그 책이 **현재 선택된 아이**의 독서기록으로 저장됩니다.
3. **책장** 탭에 표지가 그리드로 뜨는지, **기록** 탭에 평점·기분·메모가 담긴 목록으로 뜨는지 확인합니다.
4. 더보기에서 다른 아이로 전환한 뒤 책을 하나 더 등록하면, 책장/기록이 그 아이 것만 따로 보이는지 확인합니다 (같은 부모 계정이라도 아이별로 책장이 분리됩니다).

로컬 Postgres로 이 시나리오(아이 두 명, 각각 다른 책 등록, 전환 시 책장 분리)와 보안 검증(다른 부모의 아이로 `active_child_id`를 바꿔치기하면 RLS가 막는지)을 실제 쿼리로 확인했습니다.

**아직 없는 것**: 사진·음성 기록 (`reading_records.photo_url`/`voice_url` 컬럼은 이미 있지만, 파일 업로드용 Supabase Storage 버킷 설정과 촬영/녹음 UI는 다음 단계에서 추가합니다).

책 등록 화면에는 **"ISBN 없이 제목만으로 등록"** 옵션도 있습니다 — 카카오 책 검색에 없는 오래된 책·수제책 등을 위한 경로로, `book_isbns`에 아무것도 남기지 않고 `books.source`를 `manual`로 저장합니다.

### 9. Phase 4 확인: 그룹 + 추천도서

**그룹 만들기** (교사/큐레이터 계정, 추천 탭 → "+ 그룹 만들기"):

1. 그룹 이름·유형(유치원/학교/도서관/가족/커뮤니티/크리에이터)·가입 방식(승인제/공개)을 정해 만듭니다.
2. 만들면 자동으로 그 그룹의 운영자(교사/큐레이터)로 등록되고, "추천도서"라는 기본 목록이 함께 생깁니다.
3. **승인제** 그룹이면 그룹 상세 화면에 **초대 코드**(6자리)가 보입니다 — 이 코드를 학부모에게 공유합니다.

**부모가 가입하기** (추천 탭):

- **승인제 그룹**: "초대 코드로 참가하기"에 코드를 입력하면 그룹 이름이 뜨고, "가입 신청" 누르면 대기 상태로 들어갑니다. 승인 전에는 그 그룹의 추천도서가 안 보여야 합니다.
- **공개 그룹**: "둘러보기" 목록에 바로 뜨고, "팔로우" 누르면 승인 없이 즉시 가입됩니다.

**교사가 승인 + 책 추가** (그룹 상세 화면, 운영자로 접속):

1. "가입 승인 대기"에 뜬 아이 이름을 승인하면, 그 부모 쪽에서 그룹/추천도서가 보이기 시작하는지 확인합니다.
2. "책 추가"에서 **책 제목으로 검색**(카카오 API, ISBN 없이 키워드 검색)하거나 "검색에 안 나오는 책이에요"로 수동 입력해 추천도서 목록에 책을 추가합니다.

로컬 Postgres로 이 전체 시나리오(승인제 그룹 생성 → 초대 코드 조회 → 가입 신청 → 승인 → 추천도서 조회, 그리고 공개 그룹 즉시 팔로우)와 격리 검증(가입하지 않은 다른 부모 계정에는 그룹·추천도서가 안 보이는지)을 실제 쿼리로 확인했습니다.

**아직 없는 것**: 그룹당 추천도서 목록은 1개로 고정(다중 목록 관리 없음).

### 10. 책장 보기 방식 (책등 / 전면) + 검색

**책장 탭:**

1. 상단 검색창에 책 제목이나 작가 일부를 입력하면 실시간으로 걸러지는지 확인합니다.
2. 검색창 오른쪽 토글로 **전면 책장**(표지 그리드, 기존 방식)과 **책등 책장**(색깔별 책등이 나란히 꽂힌 모양)을 전환합니다. 마지막으로 고른 보기 방식은 브라우저에 저장되어 다음에 열 때도 유지됩니다.
3. 책장 상단에 "OO의 발자국 N개"로 지금까지 읽은 책 수가 표시되는지 확인합니다.

### 11. Phase 5 확인: 독서숙제

**교사가 숙제 만들기** (그룹 상세 화면, 운영자로 접속, 추천도서가 최소 1권 등록돼 있어야 함):

1. "숙제" 섹션의 **"+ 숙제 만들기"**를 누르고 제목·안내문구·기간(선택)을 입력합니다.
2. **읽을 책 고르기**에서 추천도서 목록 중 이번 숙제에 포함할 책을 체크합니다(최소 1권 필수).
3. 필요하면 **"+ 질문 미션"**(예: "가장 기억에 남는 장면은?")이나 **"+ 낭독 미션"**을 추가합니다. 음성 녹음은 아직 준비 중이라 낭독 미션은 안내 문구만 뜨고 실제 녹음 기능은 없습니다.
4. "숙제 만들기"를 누르면 그룹 상세 화면의 "숙제" 목록에 바로 뜨는지 확인합니다.

**부모가 숙제 확인하기** (오늘 탭, 아이가 그 그룹에 승인된 상태여야 함):

1. 숙제 기간(시작일~종료일) 안이면 오늘 탭에 그 숙제가 그룹 이름과 함께 뜹니다.
2. 각 책 옆의 **"다 읽었어요"**를 누르면 평점 없이 바로 독서기록이 생기고(5초 안에 기록한다는 핵심 목표 반영), 책장/기록 탭에도 나타나는지 확인합니다. 나중에 기록 탭에서 평점·메모를 채워 넣을 수 있습니다.
3. 질문 미션이 있으면 답을 입력하고 저장 → "수정"으로 다시 바꿀 수 있는지 확인합니다.
4. 숙제 기간이 지나거나 아직 승인 대기 중이면 오늘 탭에 뜨지 않아야 합니다.

로컬 Postgres로 `assignment_mission_responses` 테이블의 RLS(보호자는 자기 아이 답변만 CRUD, 교사는 자기 그룹 답변을 조회만 가능, 다른 부모는 전혀 접근 불가)와, "다 읽었어요" 기록 후 `assignment_completion` 뷰가 실제로 완료로 바뀌는지를 실제 쿼리로 확인했습니다.

### 12. Phase 6 확인: 교사 / 큐레이터 대시보드

**교사 계정**: 더보기 탭 → "교사 대시보드로 가기"(또는 로그인 후 오늘 탭에서 안내). 운영 중인 그룹마다 멤버 수·승인 대기 건수·숙제별 완료 현황(완료 X/Y)이 보이는지 확인합니다.

**큐레이터 계정**: 더보기 탭 → "큐레이터 대시보드로 가기". 발행한 그룹마다 팔로워 수와 추천도서 권수가 보이는지 확인합니다.

두 대시보드 모두 `role`이 다르면(예: 부모 계정으로 `/teacher` 접속) "교사/큐레이터 계정에서만 볼 수 있는 화면이에요" 안내만 뜨고 데이터는 안 보여야 합니다.

### 13. 사진·음성 업로드 확인

`supabase/migrations/0007_phase_storage.sql`과 `0008_remove_teacher_voice_access.sql`을 순서대로 SQL Editor에서 실행해야 합니다 — 0007이 비공개 Storage 버킷 `reading-media`를 만들고 RLS를 걸고(버킷 생성도 SQL로 되고, 별도로 대시보드에서 만들 필요는 없습니다), 0008이 교사에게 줬던 낭독 미션 녹음 조회 권한을 다시 뺍니다.

**책 등록에 사진·음성 첨부** (`app/library/add` 기록 단계):

1. "사진 첨부"로 이미지를 고르면 미리보기가 뜨고, "기록 남기기"를 누르면 실제로 업로드된 뒤 저장됩니다.
2. "음성 기록"은 온보딩에서 "음성 독서기록 녹음 및 보관에 동의합니다"에 체크했을 때만 보입니다 — 체크 안 했다면 안 보이는 게 정상입니다. 마이크 권한을 허용하고 녹음 후 "저장 중..." 없이 바로 "다시 녹음" 버튼과 재생 컨트롤이 뜨는지 확인합니다.
3. 저장 후 **기록** 탭에서 그 책 카드에 사진 썸네일과 오디오 플레이어가 뜨는지 확인합니다. 새로고침해도 계속 재생 가능해야 합니다(매번 새 서명된 URL을 발급합니다).

**낭독 미션 녹음** (오늘 탭, 교사가 낭독 미션이 포함된 숙제를 만든 뒤):

1. 음성 동의를 한 부모 계정으로 오늘 탭에 들어가면 낭독 미션에 "낭독 시작" 버튼이 보입니다.
2. 녹음 후 재생까지 확인하고, 페이지를 새로고침해도 저장된 녹음이 그대로 재생되는지 확인합니다.
3. 같은 미션을 다시 녹음하면 이전 파일이 새 파일로 교체되는지(누적되지 않는지) 확인합니다.
4. 음성 동의를 안 한 부모 계정으로는 "온보딩에서 음성 녹음에 동의하지 않으셨어요"만 뜨고 녹음 버튼 자체가 없어야 합니다.

로컬 Postgres로 `storage.objects`를 최소 스키마로 모킹해 보호자는 자기 아이 폴더 전체를 CRUD, 교사는 어떤 녹음·사진에도 접근 불가, 다른 부모는 완전 차단(조회 0건, 업로드 거부)까지 실제 쿼리로 확인했습니다. (처음엔 교사에게 낭독 미션 녹음 조회 권한을 줬었는데, "교사는 뭘 읽었나/언제 읽었나만 알면 된다"는 판단으로 다시 뺐습니다 — 0008 마이그레이션.)

### 14. 질문 은행 확인

`supabase/migrations/0009_book_questions.sql`을 SQL Editor에서 실행해야 합니다 — 레거시 앱의 범용 질문 10개를 시드 데이터로 넣습니다. AI API 호출이 전혀 없는 정적 질문 목록이라 비용이 들지 않습니다.

**책 등록 화면** (`app/library/add` 기록 단계):

1. 부모 메모 입력란 위에 "오늘의 질문"으로 질문 하나가 무작위로 뜨는지 확인합니다.
2. "다른 질문"을 누르면 다른 질문으로 바뀌는지, "+ 질문 추가"로 새 질문을 적어 저장하면 그 질문이 바로 표시되는지 확인합니다.

**숙제 만들기 화면** (그룹 상세, 운영자로 접속):

1. "+ 질문 미션"을 추가하면 입력란 아래에 질문 은행의 질문들이 칩(버튼) 형태로 뜨는지 확인합니다.
2. 칩을 누르면 입력란이 그 텍스트로 채워지는지, 입력란에 은행에 없는 새 질문을 직접 타이핑해서 숙제를 만들면 다음번에 그 질문도 칩 목록에 나타나는지 확인합니다.

로컬 Postgres로 `book_questions` RLS를 확인했습니다 — 로그인한 사용자(부모/교사 구분 없이)는 전체 질문을 조회할 수 있고, 자기 질문만 추가·삭제할 수 있으며, 다른 사람 명의로 질문을 넣거나 남의 질문을 지우는 시도는 모두 막힙니다.

### 15. 책 상태(읽고 싶어요/읽는 중/다 읽음) 확인

`supabase/migrations/0010_reading_status.sql`을 SQL Editor에서 실행해야 합니다.

**책 등록** (`app/library/add`):

1. 기록 단계 맨 위 "지금 상태"에서 세 버튼 중 하나를 고를 수 있는지 확인합니다(기본값은 "다 읽었어요").
2. "읽고 싶어요"나 "읽는 중이에요"를 고르면 평점·기분·즐겨찾기·오늘의 질문 섹션이 사라지고, 저장 버튼 문구도 "읽고 싶은 책으로 저장"처럼 바뀌는지 확인합니다. 사진·음성·메모는 그대로 남아 있어야 합니다.

**책장 탭**:

1. 상단 필터 칩(전체/읽고 싶어요/읽는 중/다 읽음)으로 걸러지는지 확인합니다. 검색·책등/전면 보기와 함께 써도 정상 동작해야 합니다.
2. "읽고 싶어요"/"읽는 중" 책 카드에만 작은 배지가 뜨고, "다 읽음" 책에는 배지가 없는지 확인합니다.
3. "OO의 발자국 N개"가 "다 읽음" 책만 세는지 확인합니다 — 읽고 싶은 책을 추가해도 숫자가 그대로여야 합니다.

**기록 탭**:

1. 기록 카드를 누르면 뜨는 수정 모달에서 상태를 언제든 바꿀 수 있는지 확인합니다(16번 항목 참고). 바꾸면 책장 탭의 필터·배지에도 반영돼야 합니다.

**숙제와의 연동** (교사가 이 책을 필독서로 낸 숙제가 있는 경우):

1. 아이 책이 "읽고 싶어요"나 "읽는 중"이면 오늘 탭·교사 대시보드에서 그 책이 아직 "완료"로 안 잡혀야 합니다.
2. 상태를 "다 읽음"으로 바꾸면 그제서야 완료로 잡히는지 확인합니다.

로컬 Postgres로 `reading_records.status`를 want→reading→done 순서로 바꿔가며 `assignment_completion` 뷰의 `completed` 값이 매번 맞게 바뀌는지, 그리고 다른 부모가 남의 아이 기록 상태를 바꾸려는 시도가 막히는지(0건 UPDATE) 확인했습니다.

### 16. 레거시 대조 점검 반영 확인

`supabase/migrations/0011_teacher_view_children.sql`을 SQL Editor에서 실행해야 합니다 — 교사가 아이 이름을 못 보던 버그(승인 대기 목록 등)를 고칩니다.

**기록 수정** (책장 탭 카드 클릭 / 기록 탭 카드 클릭 / 오늘 탭 최근 기록 클릭):

1. 아무 책이나 클릭하면 하단에서 수정 모달이 올라오는지 확인합니다.
2. 상태·평점·기분·즐겨찾기·메모를 바꾸고 "저장"을 누르면 실제로 반영되는지(책장/기록 탭에 새로고침 후 보이는지) 확인합니다.

**기록 탭 개편**: 검색창에 제목/작가 일부를 입력하면 걸러지는지, 월별로 묶여서 "2026년 8월 · N권" 형태로 보이는지, 상단에 "총 N권 · 평균 O점" 요약이 뜨는지 확인합니다.

**책장 탭**: 카드를 클릭하면 수정 모달이 열리는지, 검색/상태 필터 옆의 정렬 드롭다운(최신순/제목순/작가순)이 동작하는지 확인합니다.

**오늘 탭**: 상단에 읽은 책/이번 달/최장 연속 통계 카드가 뜨는지, 숙제 아래에 "최근 기록"이 최대 5권 뜨고 클릭하면 수정 모달이 열리는지 확인합니다.

**배지**: 책장 탭 헤더의 "발자국 N개 · 배지 보기"를 눌러 `/badges`로 이동 → 지금까지 기록/연속/재독/사진·음성 여부에 맞는 배지가 색으로 켜져 있는지 확인합니다(레거시처럼 이모지가 아니라 리본 아이콘 하나로 통일돼 있습니다).

**책 등록 흐름 통합**: 책장 탭 "+ 책 등록" 또는 기록 탭 "책 기록하기"로 들어가면, 이전처럼 선택 화면이 따로 없이 바로 "제목·저자 검색 / 바코드 스캔 / ISBN 입력" 토글이 뜨는지 확인합니다. 제목 검색으로 부모도 책을 찾을 수 있는지(이전엔 교사만 가능했음) 확인합니다.

**교사/큐레이터 전용 메뉴** (교사·큐레이터 계정으로 로그인):

1. 하단 탭이 오늘/책장/기록이 아니라 **대시보드 / 아이 관리 / 숙제 / 그룹 / 더보기**(교사) 또는 **대시보드 / 그룹 / 더보기**(큐레이터)로 바뀌는지 확인합니다. 로그인 직후 잠깐 부모 탭이 보였다가 바뀌는 건 정상입니다(역할 확인 중).
2. **아이 관리** 탭에서 운영 중인 그룹별로 아이 이름과 아바타, 숙제 완료 현황(완료 X/Y)이 뜨는지 확인합니다 — 이전에는 RLS 버그로 이름이 비어 보였을 수 있는 부분입니다.
3. **숙제** 탭에서 여러 그룹에 걸친 숙제가 그룹 이름과 함께 한 목록으로 뜨는지 확인합니다.

로컬 Postgres로 새 `children` 정책(교사는 자기 그룹 아이 이름 조회 가능, 무관한 부모는 여전히 차단)을 확인했습니다.

### 17. 하단 탭 개편 + '기록 남기기' 확인

마이그레이션 없이 화면 구성만 바뀐 변경입니다.

1. 아무 탭 화면에서든 우측 상단에 "더보기" 아이콘이 항상 떠 있는지 확인합니다(로그인/회원가입/온보딩 화면에는 안 보이는 게 정상).
2. 하단 탭이 **오늘 / 책장 / 기록 / 추천 / 배지**로 5개인지, "더보기"가 하단 탭에서 빠졌는지 확인합니다.
3. 우측 상단 "더보기"를 눌러 기존 더보기 화면(프로필/아이 관리/로그아웃 등)이 그대로 뜨는지 확인합니다.
4. 하단 탭의 "배지"를 눌러 `/badges`로 바로 이동하는지, 헤더가 "배지"(다른 탭과 같은 스타일)로 바뀌었는지 확인합니다.
5. 책장 탭 "+ 기록 남기기" 또는 기록 탭 "책 기록하기" 버튼으로 들어가면 화면 제목이 "기록 남기기"로 뜨는지 확인합니다("책 등록"이라는 문구가 어디에도 안 남아있어야 합니다).
6. 교사/큐레이터 계정으로 로그인해 하단 탭이 각각 4개(대시보드/아이 관리/숙제/그룹), 2개(대시보드/그룹)로 줄고, 우측 상단 "더보기"로 대시보드 링크·로그아웃에 여전히 접근 가능한지 확인합니다.

### 18. 기록 남기기 = 한 화면 + 기록 탭 리스트 개편 확인

마이그레이션 없이 화면 구성만 바뀐 변경입니다.

1. 책장 탭 "+ 기록 남기기" 또는 기록 탭 "책 기록하기"를 누르면, 검색 화면이 아니라 곧장 기록 화면(제목 입력이 맨 위)이 뜨는지 확인합니다.
2. 제목 입력창에 책 이름을 두 글자 이상 입력하면 잠깐 후 입력창 바로 아래에 후보 목록(표지+제목+저자)이 뜨는지 확인합니다. 후보를 클릭하면 제목/저자/표지가 자동으로 채워지고 목록이 닫히는지 확인합니다.
3. 후보를 클릭하지 않고 아무 제목이나 직접 입력한 채로 저장해도 정상적으로 기록되는지(수동 등록) 확인합니다.
4. "바코드로 찾기" / "ISBN으로 찾기"를 누르면 그 자리에서 스캐너/입력란이 펼쳐지고, 조회에 성공하면 제목/저자/표지가 채워지는지 확인합니다.
5. 기록 탭에서 같은 달에 기록이 여러 권 있을 때, 각 책이 개별 카드가 아니라 하나의 박스 안에 얇은 구분선으로 나뉘어 나오는지 확인합니다(레거시 앱과 비슷한 리스트 느낌).

### 19. 오늘 탭 숙제 카드 + 최근 기록 박스화 + 글자 크기 확인

마이그레이션 없이 화면 구성만 바뀐 변경입니다(교사 계정으로 숙제를 만들어 책 여러 권과 질문/낭독 미션을 넣어둔 상태에서 확인하면 좋습니다).

1. 오늘 탭에 "오늘의 숙제" 제목이 붙어 있는지, 숙제 카드에 더 이상 질문(텍스트 입력창)이 뜨지 않는지 확인합니다. 낭독 미션(있다면)은 그대로 뜨는지 확인합니다.
2. 숙제 카드 헤더 우측에 "N/M 완료" 배지가 뜨고, 모든 책을 다 읽으면 초록색으로 바뀌는지 확인합니다.
3. 아직 안 읽은 숙제 책을 누르면 검색 화면 없이 곧장 기록 남기기 화면이 뜨고, 제목·저자·표지가 이미 채워져 있는지 확인합니다. 저장 후 오늘 탭으로 돌아오면 그 책이 "읽었어요"로 바뀌고 완료 배지 카운트가 올라가는지 확인합니다.
4. "최근 기록"이 기록 탭처럼 하나의 박스 안에 구분선으로 나뉘어 나오는지 확인합니다.
5. 앱 전체적으로 글자와 버튼이 이전보다 살짝 커졌는지(아이가 보기 편한 정도로) 확인합니다.

### 20. 책장 중복 제거 + 출처 필터·정렬 + 숙제 요약 분리 확인

마이그레이션 없이 화면 구성만 바뀐 변경입니다. 같은 책을 두 그룹(예: 학급 A, 도서관 B)의 숙제로 각각 완독 기록해두면 확인하기 좋습니다.

1. 같은 책을 여러 그룹 숙제로 기록해도 책장에는 표지가 한 장만 뜨는지 확인합니다("발자국 N개"도 책 수 기준으로 세는지).
2. 책장/기록 탭 검색창 아래에 "전체 출처 / 직접 기록 / (그룹명)" 칩이 뜨는지, 특정 그룹을 선택하면 그 그룹 기록만 보이는지 확인합니다.
3. 기록 탭에서는 같은 책이 여러 그룹 기록으로 있으면 각각 별도 행으로(합쳐지지 않고) 보이는지, 각 행에 그룹명이 작게 붙는지 확인합니다.
4. 기록 탭 정렬 드롭다운에서 "최신순"은 기존처럼 월별 박스, "제목순"/"작가순"은 월 구분 없이 하나의 박스로 정렬되어 보이는지 확인합니다.
5. 오늘 탭의 "오늘의 숙제"가 그룹명+제목+"N/M 완료" 배지만 있는 요약 목록인지(책 목록이나 낭독 녹음 UI가 안 보이는지) 확인하고, 항목을 누르면 `/today/assignments`로 이동해 원래의 상세 숙제 카드(책마다 기록하기 링크, 낭독 미션)로 스크롤되는지 확인합니다.
6. 같은 그룹의 숙제가 두 개 이상이면, 그룹명이 한 번만 뜨고 그 아래 숙제 제목들이 구분선 없이 이어지는지, 다른 그룹으로 넘어갈 때만 구분선이 있는지 확인합니다.
7. `/today/assignments`에서 완료(읽었어요)로 표시된 숙제 책을 눌렀을 때 수정 모달이 뜨고, 평점·기분·메모를 고쳐 저장하면 반영되는지 확인합니다.

### 21. 부분 읽기 숙제 + 상태 무관 기록 + 레거시 기록장 UI 확인

`supabase/migrations/0012_partial_read.sql`을 SQL Editor에서 실행해야 합니다 — `assignment_books.target_page`, `reading_records.pages_read` 컬럼과 부분 읽기를 인정하는 `assignment_completion` 뷰가 추가됩니다.

1. 교사 계정으로 숙제를 만들 때, 책을 선택하면 그 아래 "OO쪽까지 (비워두면 완독이 기준)" 입력란이 뜨는지 확인하고, 예를 들어 30을 입력해 숙제를 만듭니다.
2. 부모 계정에서 그 책을 "읽는 중" 상태로 20쪽까지 기록하면 숙제가 아직 완료로 안 잡히고, 35쪽으로 고치면 완료로 바뀌는지 확인합니다(`/today/assignments`의 완료 카운트, 교사 대시보드의 완료 X/Y).
3. 기록 남기기(`/library/add`)와 기록 수정 모달 양쪽에서: 평점이 레거시처럼 색깔 스티커(최고/재밌어/좋아/보통/별로) 모양으로 뜨는지, "언제 읽었어?" 아래 오늘/어제/그제 버튼과 날짜 직접 입력이 뜨는지 확인합니다.
4. 상태를 "읽고 싶어요"나 "읽는 중"으로 두어도 평점·기분·즐겨찾기·오늘의 질문 입력이 계속 보이고, 그 위에 "평점·기분 같은 나머지 기록은 다 읽고 나서 채워도 괜찮아요" 안내가 뜨는지 확인합니다. "읽는 중"일 때는 "몇 쪽까지 읽었어?" 입력란도 함께 뜹니다.
5. `/today/assignments`에서 질문 미션이 있는 숙제를 열어, 질문과 답변 입력창이 다시 보이는지 확인합니다(오늘 탭 요약에는 안 보여야 정상).
6. 화면 곳곳의 제목 글자가 본문보다 좀 더 진하게 보이는지, 둥근 카드 박스들에 은은한 그림자가 생겨 배경과 구분되는지 확인합니다.

### 22. 상단 고정 타이틀 + 오늘 탭 위계 + 추천 탭 분야별 목록 + 버튼 색상 확인

`supabase/migrations/0013_book_categories.sql`을 SQL Editor에서 실행해야 합니다 — 책 분야(카테고리) 테이블이 추가됩니다.

1. 오늘/책장/기록/배지/추천 아무 탭이나 들어가서, 화면 최상단에 "OO의 책숲"(등록된 아이 이름)이 스크롤해도 안 움직이고 고정돼 있는지, 같은 줄 우측에 '더보기'가 있는지 확인합니다.
2. 오늘 탭의 "오늘의 숙제"/"최근 기록" 박스 안 글자·표지가 좀 더 작아졌는지, 구분선이 연해지고 박스 테두리까지 안 닿는지 확인합니다.
3. 교사 계정으로 그룹 상세 화면에서 "책 추가"로 책을 검색해 후보를 고르면, 바로 추가되지 않고 분야 칩(한글/수학/과학/인성/세계/창작/명작)과 "필독 도서로 표시" 체크박스가 있는 확인 화면이 뜨는지, 여기서 "목록에 추가"를 눌러야 실제로 추가되는지 확인합니다.
4. 그룹 상세 화면의 "추천도서" 섹션이 "N/M권 · P%" 진행률 카드 + 전체/필독/분야별 필터 칩 + 분야별로 묶인 목록으로 바뀌었는지 확인합니다. 부모 계정으로 들어가서, 아직 안 읽은 책 옆의 "책장에 꽂기"를 누르면 내 책장(책장 탭, "직접 기록"이 아니라 그 그룹 이름으로 필터링)에 뜨는지 확인합니다.
5. 책장 탭 버튼이 "+ 책장에 책 꽂기"로, 기록 탭 버튼이 한 줄 전체 너비의 큰 주황색 "+ 책 기록하기"로 바뀌었는지, 오늘 탭 상단에도 같은 주황 버튼이 추가됐는지 확인합니다.
6. (선택) `sangwkk@naver.com`으로 `/signup`에서 회원가입한 뒤, `supabase/seed/sample_haba7.sql`을 SQL Editor에서 실행하면 그 계정이 "하바 7세반" 교사가 되고 '알사탕'(필독)·'이상한 손님'이 추천도서로 등록되는지 확인합니다.

### 23. 로딩 스켈레톤 확인

마이그레이션 없이 화면 구성만 바뀐 변경입니다.

1. 탭을 전환하거나(오늘/책장/기록/추천/배지) 그룹 상세, 더보기, 교사/큐레이터 대시보드로 들어갈 때, 화면이 하얗게 비었다가 갑자기 뜨는 대신 회색 펄스 스켈레톤이 잠깐 보였다가 실제 내용으로 바뀌는지 확인합니다.
2. 이 동안에도 하단 탭 강조 표시(초록 밑줄)는 누르자마자 바로 바뀌는지 확인합니다(스켈레톤은 본문 영역에만 뜸).

### 24. 탭 전환 속도 개선 확인

마이그레이션 없이 코드만 바뀐 변경입니다.

1. 로그인한 상태에서 오늘 → 책장 → 기록 → 추천 → 배지 탭을 여러 번 오가며, 이전보다 화면이 뜨는 속도가 확실히 빨라졌는지 확인합니다(스켈레톤이 아주 잠깐만 보이거나 거의 안 보일 정도로 빨라져야 정상입니다).
2. 로그아웃 후 다른 계정(또는 같은 계정)으로 다시 로그인해도 탭 이동이나 온보딩 판단이 이상 없이 정상 동작하는지 확인합니다(로그아웃 시 내부 캐시 쿠키를 지우는 로직이 잘 동작하는지 간접 확인).
3. 다자녀 계정에서 더보기 → 아이 관리로 아이를 바꿔가며 책장/기록에 정확히 그 아이 기준 데이터가 뜨는지 확인합니다(활성 아이 조회 쿼리를 합치면서 로직이 안 바뀌었는지 확인하는 차원).

### 25. 기록 남기기 사진·음성 섹션 리네이밍 + 구분선 확인

마이그레이션 없이 화면 구성만 바뀐 변경입니다.

1. 기록 탭 또는 책장 탭에서 "+ 책 기록하기"/"+ 책장에 책 꽂기"로 기록 남기기 화면에 들어가, 맨 아래 카드 제목이 "{등록된 아이 이름}의 기록"으로 뜨는지 확인합니다(아이가 없으면 "우리 아이의 기록"으로 대체).
2. 그 카드 안에서 "인상 깊었던 장면을 사진으로 남겨보세요" 문구 아래 "장면 찍어 담기" 버튼으로 사진이 정상적으로 첨부/미리보기/삭제되는지 확인합니다.
3. 온보딩에서 음성 녹음에 동의한 계정이라면, 같은 카드 안에 "오늘 읽은 소감을 목소리로 남겨보세요" 문구 아래 "{아이 이름}의 목소리로 남기기" 버튼이 뜨고, 눌러서 녹음 시작 → 중지 → 재생 → 다시 녹음까지 정상 동작하는지 확인합니다. 동의하지 않은 계정은 이 섹션 자체가 안 보여야 합니다.
4. 책 찾기 / 상태·날짜 / 평점·기분·즐겨찾기 섹션 사이사이에 옅고 얇은 구분선이 보이는지, 저장(기록하기) 자체는 이전과 동일하게 정상 동작하는지 확인합니다.
5. 기록 탭 또는 책장 탭에서 기존 기록 카드를 눌러 "기록 고치기" 모달을 열고, 상태/날짜 섹션과 평점/기분 섹션 사이에도 같은 스타일의 옅은 구분선이 보이는지 확인합니다(이 모달에는 사진·음성 섹션이 없습니다 — 기록 남기기 화면에만 있는 기능입니다).
6. `components/assignment-today.tsx`의 낭독 미션에서 음성 녹음 버튼 문구("낭독 시작")가 이전과 동일하게 뜨고 정상 동작하는지 확인합니다(공용 컴포넌트를 함께 고쳤으니 회귀가 없는지 확인하는 차원).

### 26. 국립어린이청소년도서관 사서추천도서 자동 연동 확인

마이그레이션 0014(`book_list_items` 유니크 제약)를 먼저 적용하고, 위 "3-1. 국립어린이청소년도서관 사서추천도서 API 연동" 단계를 마친 뒤 확인합니다.

1. `nlcy_curator.sql` 실행 후 `/recommend`에서 (부모 계정으로) "국립어린이청소년도서관"이 승인 없이 바로 보이고 팔로우되는 공개 그룹으로 뜨는지 확인합니다(유형 라벨은 "도서관"으로 뜹니다).
2. 시드한 큐레이터 계정으로 로그인해 `/curator`에 들어가면, "국립어린이청소년도서관" 카드에만 안내 문구와 "지금 동기화" 버튼이 뜨는지 확인합니다(다른 큐레이터 그룹에는 안 뜸).
3. "지금 동기화"를 누르고, `NLCY_SERVICE_KEY`가 올바르면 몇 초 후 "N권 추가, M권은 이미 있어서 건너뜀" 메시지가 뜨는지 확인합니다. `추천도서` 페이지(`/recommend/[groupId]`)에 실제로 책들이 채워지는지 확인합니다.
4. 같은 버튼을 한 번 더 눌러 두 번째 동기화를 돌렸을 때, "추가"는 0에 가깝고 "건너뜀"이 이전 동기화 권수만큼 뜨는지 확인합니다(같은 책이 중복으로 쌓이지 않는지 확인하는 차원).
5. 환경변수를 일부러 비워두고 버튼을 누르면(로컬 테스트 시) 화면에 어떤 값이 빠졌는지 알려주는 에러 메시지가 뜨는지 확인합니다.
6. `CRON_SECRET` 헤더 없이 `/api/cron/nlcy-sync`를 직접 호출하면 401이 뜨는지 확인합니다(외부에서 함부로 못 부르는지 확인하는 차원). Vercel 배포 후에는 `vercel.json`에 등록된 Cron이 매일 자동으로 같은 라우트를 부릅니다.

### 27. 계정 전환 시 하단 탭이 이전 계정 것으로 남는 버그 확인

마이그레이션 없이 코드만 바뀐 변경입니다(NLCY 큐레이터 계정을 테스트하다가 실제로 발견한, NLCY와는 무관한 기존 버그).

1. 큐레이터 계정으로 로그인해 `/curator`에 들어간 뒤(하단 탭이 대시보드/그룹으로 뜨는 것 확인), 로그아웃하고 부모 계정으로 다시 로그인합니다.
2. 오늘 탭 내용은 정상인데 하단 탭만 큐레이터용(대시보드/그룹)으로 남아있지 않고, 부모용 5탭(오늘/책장/기록/추천/배지)으로 바로 바뀌는지 확인합니다.
3. 반대 방향(부모 → 교사, 교사 → 큐레이터 등)도 한 번씩 로그인/로그아웃을 반복하며 하단 탭이 매번 새 계정의 역할과 맞게 뜨는지 확인합니다.

### 28. 전반적인 속도 개선(이중 인증 검증 제거 + N+1 쿼리 제거) 확인

마이그레이션 없이 코드만 바뀐 변경입니다. 인증 검증 경로 자체를 건드린 변경이라 꼼꼼히 확인해 주세요.

1. **로그인 상태 확인**: 로그인 안 한 상태로 `/today`, `/library`, `/onboarding` 등에 들어가면 여전히 `/login`으로 보내지는지 확인합니다. 로그인 후 온보딩 미완료 상태로 탭에 들어가면 `/onboarding`으로, 온보딩 완료 후 `/login`·`/signup`에 들어가면 `/today`로 보내지는지도 예전과 동일하게 동작하는지 확인합니다(이 리다이렉트 로직 자체를 재구성했습니다).
2. **다른 사용자 데이터 접근 차단**: 부모 A로 로그인해 책장·기록·더보기가 A의 데이터만 보이는지 확인합니다(다른 사용자 id가 실수로 섞여 들어가면 바로 티가 나는 화면들입니다).
3. **교사/큐레이터 대시보드 숫자 확인**: 그룹을 2개 이상 운영하는 교사 계정으로 `/teacher`, `/teacher/children`, `/teacher/assignments`에 들어가 멤버수·승인대기·숙제 완료 현황이 그룹별로 정확히 나뉘어 뜨는지 확인합니다. 특히 **같은 아이가 두 그룹에 모두 속해 있는 경우**, `/teacher/children`에서 그 아이의 "완료 X/Y"가 그룹마다 다르게(그 그룹 숙제 기준으로만) 뜨는지 확인합니다 — 이번에 고친 버그가 다시 생기지 않는지 확인하는 핵심 지점입니다. `/curator`도 그룹이 2개 이상인 계정으로 팔로워수·추천도서수가 맞게 뜨는지 확인합니다.
4. **로그아웃 시 세션 쿠키 정리**: 로그아웃 후 로그인 화면으로 잘 돌아가고, 다른 계정으로 다시 로그인해도 온보딩 판단이나 세션이 꼬이지 않는지 확인합니다(리다이렉트 응답에 쿠키가 누락되던 예전 버그가 재발하지 않는지 확인하는 차원).
5. **체감 속도**: 탭 전환·대시보드 진입이 이전보다 더 빨라졌는지 확인합니다(페이지마다 인증 왕복이 하나씩 줄었으므로 특히 첫 진입/새로고침 시 체감돼야 합니다). 배포 직후 첫 몇 번은 서버 함수 콜드 스타트로 오히려 느려 보일 수 있으니, 1~2분 정도 지난 뒤에 다시 확인해 주세요.

### 29. 기록 고치기 모달의 사진·음성 수정 확인

마이그레이션 없이 코드만 바뀐 변경입니다.

1. 책장/기록/오늘/오늘의 숙제 네 화면 모두에서, 사진이나 음성이 있는 기존 기록을 눌러 "기록 고치기" 모달을 열면 맨 아래 "{아이 이름}의 기록" 카드에 기존 사진/음성이 바로 보이는지 확인합니다(모달이 열리자마자 서명된 URL을 불러오므로 아주 잠깐 로딩 후 나타납니다).
2. "사진 바꾸기"로 새 사진을 고르면 미리보기가 새 사진으로 바뀌고, 저장 후 다시 열었을 때 실제로 새 사진으로 교체됐는지 확인합니다.
3. "사진 지우기"를 누르고 저장하면 사진이 없어지고, 책장/기록 탭에서도 사진이 더 이상 안 뜨는지 확인합니다.
4. 음성도 동일하게 "다시 녹음"/"지우기"가 정상 동작하는지 확인합니다(온보딩에서 음성 녹음에 동의한 계정만 이 섹션이 보입니다).
5. 사진·음성을 아예 안 건드리고 다른 항목(평점 등)만 고쳐서 저장했을 때, 기존 사진/음성이 그대로 남아있는지 확인합니다(부분 업데이트가 제대로 되는지 확인하는 핵심 지점입니다).

### 30. 하단 탭 재편(배지 → 숙제) + 기록 탭 압축 + 팔로우 상태 확인

마이그레이션 없이 코드만 바뀐 변경입니다.

1. 부모 계정으로 로그인해 하단 탭이 **오늘 · 책장 · 기록 · 추천 · 숙제**로 뜨는지, 상단 우측에 배지·더보기 아이콘이 나란히 뜨는지 확인합니다.
2. 숙제 탭을 눌러 진행 중/앞으로의 숙제가 그룹별 섹션으로 묶여 뜨는지, 맨 아래 "지난 숙제 보기"를 누르면 마감된 숙제만 따로 보이는지 확인합니다.
3. 오늘 탭의 "오늘의 숙제" 요약에서 "전체 보기"나 개별 숙제를 누르면 숙제 탭(`/assignments#...`)으로 이동하고, 해당 숙제 카드로 스크롤되는지 확인합니다.
4. 기록 탭에서 각 행이 작은 표지 + 제목 + 그룹명(작게) + 날짜("8월 19일" 형태) 한 줄로 뜨는지, 읽고 싶은 책/읽는 중인 책은 안 보이고 다 읽은 책만 뜨는지 확인합니다. 행을 누르면 기존과 동일하게 "기록 고치기" 모달이 열리는지 확인합니다.
5. 부모 계정으로 공개(open) 그룹을 하나 팔로우한 뒤, 그 그룹 상세 화면(`/recommend/[groupId]`)에 다시 들어가서 "팔로잉" 표시가 계속 보이는지 확인합니다(예전엔 팔로우한 뒤 이 영역이 통째로 사라졌습니다). `/recommend`의 "둘러보기" 목록에서도 같은 그룹이 "팔로잉"으로 뜨는지 확인합니다.
6. 기록 탭과 오늘 탭의 "+ 책 기록하기" 버튼이 호박색이 아니라 진한 빨강(berry)으로 뜨는지 확인합니다.

### 31. 기록 중복 저장 방지 + 기록 삭제 확인

마이그레이션 없이 코드만 바뀐 변경입니다.

1. 기록 남기기 화면(`/library/add`)에서 저장 버튼을 아주 빠르게 연타해도 기록이 한 번만 저장되는지 확인합니다(전에는 빠른 연타 시 중복 저장될 여지가 있었습니다).
2. 책장/기록/오늘/숙제 어디서든 "기록 고치기" 모달을 열면 저장 버튼 아래 "이 기록 삭제하기"(테두리 있는 회색 버튼)가 보이는지 확인합니다. 눌러서 확인창에서 확인하면 그 기록이 실제로 사라지고(책장/기록 탭에서도 없어짐), 취소하면 아무 일도 없는지 확인합니다.
3. 다른 부모 계정으로는 이 삭제 버튼을 눌러도(정상적으로는 남의 기록 자체가 안 보이지만) RLS가 막는지 — 즉 이 기능이 본인 아이의 기록에만 동작하는지 확인합니다.

### 32. 완료된 숙제가 오늘 탭에서 사라지는지 확인

마이그레이션 없이 코드만 바뀐 변경입니다.

1. 마감일(종료일)을 설정하지 않은 숙제를 하나 만들고(교사 계정, `/recommend/[groupId]`의 숙제 만들기), 그 숙제의 책을 부모 계정에서 전부 기록해 완료 처리합니다.
2. 오늘 탭으로 돌아가서 "오늘의 숙제" 요약에 그 숙제가 더 이상 안 뜨는지 확인합니다.
3. 숙제 탭(`/assignments`)에는 그 숙제가 여전히 뜨고, "N/M 완료" 배지가 100%로 표시되는지 확인합니다(관리 화면이라 완료된 것도 계속 보여야 정상입니다).
4. 아직 안 끝난 책이 하나라도 남은 숙제는 오늘 탭에 그대로 뜨는지 확인합니다.

### 33. 추천 탭 / 숙제 탭 역할 분리 확인

마이그레이션 없이 코드만 바뀐 변경입니다.

1. 그룹 상세 화면(`/recommend/[groupId]`)에 부모(비운영진) 계정으로 들어가면 추천도서 목록은 그대로 보이지만, 숙제 카드 목록은 더 이상 안 보이는지 확인합니다.
2. 같은 화면에 교사(운영진) 계정으로 들어가면 "숙제 만들기" 폼은 여전히 보이는지 확인합니다.
3. 숙제 탭(`/assignments`)에서 아이가 속한 그룹이 있으면 상단에 그룹 선택 드롭다운("전체 그룹" 포함)이 보이는지 확인합니다.
4. 드롭다운에서 특정 그룹을 고르면 그 그룹의 "OO의 추천도서" 섹션이 "숙제" 섹션 위에 뜨고, 그 추천도서 목록에서 "책장에 꽂기"가 그룹 상세 화면과 동일하게 동작하는지 확인합니다.
5. "전체 그룹"으로 되돌리면 추천도서 섹션은 사라지고 모든 그룹의 숙제가 그룹별로 다시 보이는지 확인합니다.
6. "지난 숙제 보기"를 눌렀을 때 방금 고른 그룹 필터가 `/assignments/past`에도 그대로 유지되는지 확인합니다.

### 34. 그룹 팔로우/가입 중복 방지 확인

마이그레이션 0015가 필요합니다(`supabase/migrations/0015_group_members_unique.sql`).

1. 마이그레이션을 적용하기 전 중복 `group_members` 행이 있었다면, 적용 후 그룹당 아이/운영진 하나씩만 남았는지(승인된 행이 우선 남는지) 확인합니다.
2. 추천 탭에서 공개(open) 그룹의 "팔로우" 버튼을 아주 빠르게 여러 번 눌러도 에러 없이 한 번만 팔로우되는지(숙제 탭 그룹 드롭다운에 그 그룹이 한 번만 뜨는지) 확인합니다.
3. 초대 코드로 그룹 찾기(`JoinByCode`)에서도 "가입 신청"/"팔로우" 버튼을 연타했을 때 같은 그룹에 중복 신청되지 않는지 확인합니다.
4. 숙제 탭(`/assignments`)과 지난 숙제(`/assignments/past`)의 그룹 드롭다운에 같은 그룹이 두 번 이상 뜨지 않는지 확인합니다.

## 폴더 구조

```text
/app            Next.js App Router 라우트 (탭, 인증, 온보딩 등)
/components     UI 프리미티브, 아이콘, 하단 탭 네비게이션
/lib/supabase   Supabase client/server 헬퍼
/supabase/migrations  DB 스키마 및 RLS 정책 마이그레이션
/legacy         서비스화 이전 개인용 프로토타입(참고용, 빌드 제외)
```

## 스크립트

```bash
npm run dev     # 개발 서버
npm run build   # 프로덕션 빌드
npm run start   # 프로덕션 서버 실행
npm run lint    # ESLint
```
